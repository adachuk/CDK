name: Test ProjectStack
on:
    push:
        branches:
            lucycdk
jobs:
    test-and-build:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v3

